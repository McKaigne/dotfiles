# ==============================================================================
# Basic Settings & Capabilities
# ==============================================================================
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",xterm-ghostty:RGB,ghostty:RGB,xterm-256color:RGB,*:RGB"
set -as terminal-features ",*:RGB,*:clipboard,*:cstyle"
set -g mouse on
set -s escape-time 0
set -g focus-events on
set -g set-clipboard on
set -g allow-passthrough on
setw -g aggressive-resize on
set -g detach-on-destroy off
set -g history-limit 50000

# Ghostty GLSL Cursor Shader & Sync Support
set -ga terminal-overrides '*:Ss=\E[%p1%d q:Se=\E[2 q'
set -as terminal-features ',*:sync:passthrough'

# ==============================================================================
# Prefix: Ctrl + b (Triggered via n + m in Kanata)
# ==============================================================================
unbind C-Space
unbind C-b
set -g prefix C-b
bind-key C-b send-prefix

# ==============================================================================
# 1. Pane Navigation & Splits (Zero-Prefix Alt — No Helix Conflicts)
# ==============================================================================
bind -n M-h select-pane -L
bind -n M-j select-pane -D
bind -n M-k select-pane -U
bind -n M-l select-pane -R

# Splits: Alt+v vertical split; Horizontal split moved to Prefix+s (shields Helix Alt-s!)
bind -n M-v split-window -h -c "#{pane_current_path}"
bind s split-window -v -c "#{pane_current_path}"
bind v split-window -h -c "#{pane_current_path}"

# Zoom & Close Panes
bind -n M-z resize-pane -Z
bind -n M-q kill-pane
bind x kill-pane

# Fine-tune resize (Alt + Arrows)
bind -n -r M-Up resize-pane -U 2
bind -n -r M-Down resize-pane -D 2
bind -n -r M-Left resize-pane -L 2
bind -n -r M-Right resize-pane -R 2

# ==============================================================================
# 2. Tabs (Windows) & Navigation
# ==============================================================================
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Alt + t: New tab in current directory
bind -n M-t new-window -c "#{pane_current_path}"
bind t new-window -c "#{pane_current_path}"

# Alt + Ctrl + j/k: Cycle Tabs (Honors Pinky + Ring no-gaps rule!)
bind -n C-M-j next-window
bind -n C-M-k previous-window

# Alt + Ctrl + q: Close current tab (window)
bind -n C-M-q kill-window

# Jump directly to Tab 1-10
bind -n M-1 if-shell 'tmux select-window -t :1' '' 'new-window -t :1 -c "#{pane_current_path}"'
bind -n M-2 if-shell 'tmux select-window -t :2' '' 'new-window -t :2 -c "#{pane_current_path}"'
bind -n M-3 if-shell 'tmux select-window -t :3' '' 'new-window -t :3 -c "#{pane_current_path}"'
bind -n M-4 if-shell 'tmux select-window -t :4' '' 'new-window -t :4 -c "#{pane_current_path}"'
bind -n M-5 if-shell 'tmux select-window -t :5' '' 'new-window -t :5 -c "#{pane_current_path}"'
bind -n M-6 if-shell 'tmux select-window -t :6' '' 'new-window -t :6 -c "#{pane_current_path}"'
bind -n M-7 if-shell 'tmux select-window -t :7' '' 'new-window -t :7 -c "#{pane_current_path}"'
bind -n M-8 if-shell 'tmux select-window -t :8' '' 'new-window -t :8 -c "#{pane_current_path}"'
bind -n M-9 if-shell 'tmux select-window -t :9' '' 'new-window -t :9 -c "#{pane_current_path}"'
bind -n M-0 if-shell 'tmux select-window -t :10' '' 'new-window -t :10 -c "#{pane_current_path}"'

# Rename window
bind -n M-r command-prompt -I "#W" "rename-window '%%'"

# ==============================================================================
# 3. High-Speed Popups (Zero-Prefix Alt)
# ==============================================================================
# Alt + e: nnn file manager in current directory
bind -n M-e display-popup -d "#{pane_current_path}" -w 85% -h 85% -E "@nnn@ -e"

# Alt + b: Fuzzy Window / Tab picker
bind -n M-b display-popup -E -w 75% -h 65% -T " 󰖯 Tabs & Windows " "@tmuxWindowPicker@"
bind b display-popup -E -w 75% -h 65% -T " 󰖯 Tabs & Windows " "@tmuxWindowPicker@"

# Alt + a: Project switcher (Matches Niri Mod+A!)
bind -n M-a display-popup -E -w 75% -h 65% -T " 󰒉 Sessions & Projects " "@tmuxSessionizer@"
bind f display-popup -E -w 75% -h 65% -T " 󰒉 Sessions & Projects " "@tmuxSessionizer@"

# Alt + g: Lazygit popup
bind -n M-g display-popup -d "#{pane_current_path}" -w 90% -h 85% -E "@lazygit@"
bind g display-popup -d "#{pane_current_path}" -w 90% -h 85% -E "@lazygit@"

# Alt + m: Btop system monitor
bind -n M-m display-popup -w 85% -h 80% -E "@btop@"

# Alt + Enter: Floating Nushell scratchpad
bind -n M-Enter display-popup -d "#{pane_current_path}" -w 80% -h 70% -E "@nu@"

# Interactive Cheatsheet Popup
bind -n 'M-/' display-popup -w 75% -h 75% -T " 󰌌 Tmux Keybindings " "@tmuxCheatsheet@"
bind '/' display-popup -w 75% -h 75% -T " 󰌌 Tmux Keybindings " "@tmuxCheatsheet@"

# Prefix + h: Helix pane picker (Leaves Alt+x untouched for Helix!)
bind h display-popup -E -w 80% -h 70% -T " 󰅩 Helix Panes " "@tmuxHelixPicker@"

# ==============================================================================
# 4. Emacs-Style Copy Mode & Wayland Clipboard
# ==============================================================================
set-window-option -g mode-keys vi

# Enter copy mode via Prefix + c (Shields Helix Alt-c!)
bind c copy-mode
bind -n M-PageUp copy-mode -u

# Inside copy mode: Emacs & Vi hybrid ergonomics
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi Space send-keys -X begin-selection
bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "@wlCopy@"
bind-key -T copy-mode-vi M-w send-keys -X copy-pipe-and-cancel "@wlCopy@"
bind-key -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel "@wlCopy@"
bind-key -T copy-mode-vi C-g send-keys -X cancel
bind-key -T copy-mode-vi Escape send-keys -X cancel
bind-key -T copy-mode-vi q send-keys -X cancel
bind-key -T copy-mode-vi M-v send-keys -X page-up
bind-key -T copy-mode-vi C-v send-keys -X page-down
bind-key -T copy-mode-vi C-s command-prompt -i -p "(search up)" "send -X search-backward-incremental \"%%%\""
bind-key -T copy-mode-vi / command-prompt -i -p "(search down)" "send -X search-forward-incremental \"%%%\""
unbind -T copy-mode-vi MouseDragEnd1Pane

# Config reload
unbind r
bind r run-shell 'tmux source-file "$TMUX_CONF" && tmux display "Config reloaded from Nix store"'

# Dynamic Noctalia Palette Bridge
source-file -q "$HOME/.cache/noctalia/tmux.conf"

# ==============================================================================
# 5. Status Bar Styling (Dotbar Minimalist Theme)
# ==============================================================================
set -g status "on"
set -g status-position bottom
set -g status-style "bg=default,fg=#585b70"
set -g status-bg default
set -g status-justify absolute-centre
set -g status-left-length 50
set -g status-right-length 50

# Messages
set -g message-style "fg=#cdd6f4,bg=#313244,align=centre"
set -g message-command-style "fg=#cdd6f4,bg=#313244,align=centre"

# Panes
set -g pane-border-style "fg=#313244"
set -g pane-active-border-style "fg=#89b4fa"

# Window List
set -g window-status-separator "#[fg=#45475a] • "
set -g window-status-style "fg=#585b70,bg=default"
set -g window-status-format " #I #W#{?window_zoomed_flag, 󰊓,} "
set -g window-status-current-style "fg=#cdd6f4,bold,bg=default"
set -g window-status-current-format " #I #W#{?window_zoomed_flag, 󰊓,} "
set -g window-status-activity-style "fg=#fab387,bg=default,none"

# Left & Right indicators
set -g status-left "#{?client_prefix,#[fg=#cba6f7]#[bold] 󰌘 #S ,#[fg=#89b4fa] #S }"
set -g status-right "#[fg=#585b70]%H:%M "

set -g clock-mode-colour "#89b4fa"
set -g mode-style "fg=#89b4fa,bg=#313244,bold"